{
  "name": "studyflow-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Backend API for StudyFlow — generates structured study sets via Gemini",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@google/genai": "^2.19.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
